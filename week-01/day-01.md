i learnt about what linux is and who can use it.
It's a software operating system that allows other and web browsers be installed and run on a computer, it's not only used on hardware applications but also software applications.
It can be use by anybody and its not difficult to learn, it also help understand computing better and gives a better computing skill
I learnt about the kinds of interface Linux uses.
Linux uses a GUI [GRAPHICAL USER-INTERFACE] but it has a more efficient tool for carrying out the same action which is the CLI [command line interface].
The command line interface is a text based interface that accepts commands that you type in it.
The CLI is a powerful tool that is often the primary method used to administer small low power devices, extremely capable cloud computing servers and everything in between. 
I also learnt about what a command, arguments, options, and some of the characters.
A command is a software program that when executed on the CLI performs an action on the computer.
The ls command displays a listing of information about files, the aptitude command is a package management tool availabe on some linux distribution. 
An argument can be used to specify something for the command to act upon. 
options can be used to alter the behaviour of a command. There are also two paths.
The Absolute pathallows you to specify the exact location of a directory, it always start at the root directory.
While relative path gives direction to a file relative to the current location in the file system, they start with the name of a directory.
The character [ . . ] is refered to as the parent directory, it always represents one directory higher relative to the current directory.
This character [ . ] regardless of which directory you are in, it always represents your current directory.
the character [ ~ ] the home directory of the current user is represented by the character.
Two things i found confusing ;
How to recreate the tree using one command with brace expression, i got it to a point and it started geting confusing i tried again multiple times.
The other one was building the directory tree. 
i want to master the command that can recreate the entire tree with brace expansion.
